server:
  port: 8701
spring:
  application:
    name: fehead-common-service
  # 加密配置文件
  profiles:
    active: enc
  cloud:
    nacos:
      discovery:
        server-addr: open.fehead.com:8848
      config:
        server-addr: open.fehead.com:8848
    # 配置中心相关配置
#    config:
#      discovery:
#        enabled: true
#        service-id: FEHEAD-CONFIG-SERVER
#      profile: test
#      profile: master
# 注册中心地址
#eureka:
#  client:
#    service-url:
#      defaultZone: http://open.fehead.com:8761/eureka
#      defaultZone: http://localhost:8761/eureka
